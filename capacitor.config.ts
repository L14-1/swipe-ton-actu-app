import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'swipe-ton-actu',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
